# 🚀 БЫСТРЫЙ СТАРТ ДЕПЛОЯ

## ⚡ За 5 минут на Railway.app

### Шаг 1️⃣: Проверь что всё готово

```bash
# Убедись что у тебя есть:
✅ GitHub аккаунт
✅ Telegram Bot Token (у тебя уже есть)
✅ Проект залит на GitHub
```

### Шаг 2️⃣: GitHub подготовка

```bash
# Если ещё не залил на GitHub:
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/telegram-bot.git
git push -u origin main
```

### Шаг 3️⃣: Railway регистрация

1. Перейди на **https://railway.app**
2. Нажми **"Start Project"**
3. Выбери **"Deploy from GitHub"**
4. Авторизуйся через GitHub
5. Выбери твой репозиторий `telegram-bot`

### Шаг 4️⃣: Переменные окружения

На Railway добавь переменные:

```env
BOT_TOKEN=12345:ABCDEFghijklmnop  # Твой токен бота
WEBHOOK_URL=https://app-name.railway.app
FLASK_ENV=production
DEBUG=False
```

### Шаг 5️⃣: Проверка

Через 2-3 минуты:
- Заходишь в Telegram
- Тестируешь бота: `/start`
- Открываешь Web App
- Проверяешь что работает

**✅ ГОТОВО! Бот работает 24/7! 🎉**

---

## 📋 Файлы готовые для деплоя

✅ `Procfile` - конфигурация для Railway
✅ `requirements.txt` - все зависимости (обновлен)
✅ `railway.toml` - параметры Railway
✅ `Dockerfile` - для Docker (опционально)
✅ `.gitignore` - секреты не коммитятся

---

## 🔐 ВАЖНО: Переменные окружения

### На локальной машине (для тестирования):

Создай файл `.env`:
```env
BOT_TOKEN=12345:ABCDEFghijklmnop
WEBHOOK_URL=https://192.168.1.122:5000
FLASK_ENV=development
DEBUG=True
```

### На Railway (для production):

Добавь в Dashboard → Environment:
```env
BOT_TOKEN=12345:ABCDEFghijklmnop
WEBHOOK_URL=https://your-app-name.railway.app
FLASK_ENV=production
DEBUG=False
```

⚠️ **ВАЖНО:** BOT_TOKEN должен быть одинаковым везде!

---

## 📊 Альтернативные хостинги (если Railway не подойдет)

### Render.com (альтернатива)
```bash
# 1. Перейди на render.com
# 2. "New+" → "Web Service"
# 3. Выбери GitHub репозиторий
# 4. Build: pip install -r requirements.txt
# 5. Start: python -m bot.main
```

### VPS (если нужна максимальная скорость)
```bash
# Заказать: timeweb.com, selectel.com
# Минимум: Ubuntu 20.04, 1GB RAM, 50₽/месяц
# Установить Python, клонировать проект, запустить
```

---

## 🐛 Типичные проблемы

### ❌ "Bot not responding"

**Проверь:**
- [ ] BOT_TOKEN правильный в переменных
- [ ] WEBHOOK_URL указан правильно (без трейлинг слэша)
- [ ] Логи на Railway - ищи ошибку

```bash
# На Railway смотри логи - there usually is the answer
```

### ❌ "Web App not opening"

**Проверь:**
- [ ] WEBHOOK_URL = `https://your-app-name.railway.app` (без `/web`)
- [ ] Flask сервер запущен (смотри логи)

### ❌ "Database error"

**Проверь:**
- [ ] Если SQLite - всё работает автоматически
- [ ] Если PostgreSQL - добавь "Add PostgreSQL" на Railway

---

## ✅ Финальный чек-лист перед деплоем

- [ ] Код протестирован локально (`python -m bot.main`)
- [ ] Все функции работают (/start, Web App, добавление товара)
- [ ] `.env` файл **НЕ** добавлен в git
- [ ] `requirements.txt` актуален (`pip freeze > requirements.txt`)
- [ ] GitHub репозиторий PUBLIC (для Railway)
- [ ] Telegram Bot Token получен и проверен
- [ ] Готов добавить переменные на хостинге

---

## 🎉 Поздравляю!

Теперь твой бот будет работать **24/7** на облачном хостинге! 🚀

**Нужна помощь?** Посмотри подробнее:
- 📖 `DEPLOY.md` - подробная инструкция
- 📊 `HOSTING_COMPARISON.md` - сравнение хостингов
- ✅ `DEPLOY_CHECKLIST.md` - полный чек-лист

---

## 💬 Команды для деплоя

```bash
# Подготовка проекта
python prepare_deploy.py

# Или на Windows
deploy.bat

# Или на Linux/Mac
bash deploy.sh
```

**Поехали! 🚗**
