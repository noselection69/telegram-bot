# 🚀 Деплой бота на хостинг

## 🎯 Рекомендуемые хостинги

### 1. **Railway.app** ⭐ РЕКОМЕНДУЕТСЯ
- **Плюсы:** Простой деплой через GitHub, бесплатный слой, автоматический HTTPS, поддержка WebHook
- **Цена:** Бесплатно до 500 часов/месяц
- **Время установки:** 5-10 минут

### 2. **Render.com**
- **Плюсы:** Стабильный, хороший support, бесплатный слой
- **Цена:** Free tier доступен
- **Время установки:** 10-15 минут

### 3. **VPS (Timeweb, Hoster.ru)**
- **Плюсы:** Полный контроль, дешево в России, 24/7 работа
- **Цена:** 50-150₽/месяц
- **Время установки:** 20-30 минут (Linux)

---

## 📋 Что нужно для деплоя

1. **GitHub аккаунт** (для хостинга с автодеплоем)
2. **Telegram Bot Token** (у тебя уже есть)
3. **Выбранный хостинг** (Railway, Render, VPS)
4. **PostgreSQL база** (опционально, можно SQLite)

---

## 🔧 Подготовка проекта к деплою

### ✅ Уже подготовлено:
- ✅ `requirements.txt` - обновлен с gunicorn и остальными пакетами
- ✅ `Procfile` - конфигурация для Railway/Heroku
- ✅ `railway.toml` - специфичные параметры Railway
- ✅ `.gitignore` - секреты не будут залиты
- ✅ `bot/` - структура готова

### ❌ Что нужно сделать:

1. Обновить конфиги для использования переменных окружения
2. Подготовить GitHub репозиторий
3. Настроить хостинг

---

## 📱 Пошаговый деплой на Railway.app (САМЫЙ ПРОСТОЙ)

### Шаг 1: Подготовка на GitHub

```bash
# Инициализируем git репозиторий
git init
git add .
git commit -m "Initial commit: Telegram bot ready for deployment"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/telegram-bot.git
git push -u origin main
```

### Шаг 2: Регистрация на Railway

1. Перейди на https://railway.app
2. Нажми "Start Project"
3. Выбери "Deploy from GitHub"
4. Авторизуйся с помощью GitHub
5. Выбери твой репозиторий

### Шаг 3: Настройка переменных окружения на Railway

В панели Railway добавь эти переменные:

```
BOT_TOKEN = ваш_токен_бота
WEBHOOK_URL = https://your-app-name.railway.app
DATABASE_URL = postgresql://user:password@host/db  # будет автоматически если используешь Railway PostgreSQL
FLASK_ENV = production
DEBUG = False
```

### Шаг 4: Настройка сервисов на Railway

1. **Add Service** → **GitHub Repo**
2. **Add Service** → **PostgreSQL** (если нужна база)
3. Настрой автодеплой при пуше в main

### Шаг 5: Проверка логов

Перейди в Railway Dashboard и посмотри логи:
```
✅ INFO:__main__:Database initialized
✅ INFO:__main__:Bot commands set
✅ INFO:__main__:Web server started on port 5000
✅ INFO:aiogram.dispatcher:Start polling
```

---

## 🖥️ Деплой на VPS (если выбрал VPS маршрут)

### На Timeweb/Hoster.ru:

```bash
# Подключись по SSH
ssh root@ваш_ip

# Обнови систему
apt-get update && apt-get upgrade -y

# Установи Python 3.11
apt-get install -y python3.11 python3-pip python3-venv git

# Клонируй проект
git clone https://github.com/YOUR_USERNAME/telegram-bot.git /home/bot
cd /home/bot

# Создай виртуальное окружение
python3.11 -m venv venv
source venv/bin/activate

# Установи зависимости
pip install -r requirements.txt

# Создай systemd сервис
sudo nano /etc/systemd/system/telegram-bot.service
```

Вставь это в файл:

```ini
[Unit]
Description=Telegram Financial Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/home/bot
Environment="PATH=/home/bot/venv/bin"
ExecStart=/home/bot/venv/bin/python -m bot.main
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запусти:

```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot

# Проверь статус
sudo systemctl status telegram-bot
```

---

## 🔒 Защита секретов

### ✅ Уже в .gitignore:
- `.env` файл
- `*.db` файлы
- `*.pem` сертификаты

### ❌ НИКОГДА не комитьте:
- BOT_TOKEN
- DATABASE_URL
- Приватные ключи
- Пароли

---

## 📊 Мониторинг после деплоя

### Railway Dashboard предоставит:
- ✅ Логи бота в реальном времени
- ✅ CPU/Memory использование
- ✅ Перезагрузки и ошибки
- ✅ Время работы (uptime)

### Рекомендуемые метрики для отслеживания:
- Память: должна быть < 200MB
- CPU: нормально < 10% в среднем
- Время отклика API: < 500ms

---

## 🐛 Типичные проблемы и решения

### ❌ "ModuleNotFoundError: No module named 'bot'"

**Решение:** Проверь, что `requirements.txt` содержит все зависимости:
```bash
pip freeze > requirements.txt
```

### ❌ "SSL: CERTIFICATE_VERIFY_FAILED"

**Решение:** На хостинге уже есть SSL, скачать не нужны. Просто используй переменные:
```python
SSL_CONTEXT = None  # на хостинге
```

### ❌ "Connection refused to Telegram API"

**Решение:** Это нормально в первые несколько минут. Railroad настраивает сеть. Просто жди.

### ❌ "Database is locked"

**Решение:** Мигрируй на PostgreSQL:
```bash
# На Railway автоматически через Add Service
```

---

## ✅ Финальная проверка

После деплоя проверь в Telegram:

```
1. /start - должна работать
2. Открыть Web App - должна открываться
3. Добавить товар/авто - должны сохраняться
4. Проверить аренду - должны приходить уведомления
```

---

## 🎉 Готово!

Теперь твой бот работает 24/7 на облачном хостинге! 🚀

По вопросам деплоя - пиши, помогу! 📞
