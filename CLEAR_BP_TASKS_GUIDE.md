# Удаление и переинициализация BP заданий на Railway

## 🛠️ Способ 1: Через скрипты Python (РЕКОМЕНДУЕТСЯ)

### Вариант A: Только очистить таблицу

```bash
# Локально (если DATABASE_URL настроена)
python clear_bp_tasks.py

# Или на Railway через terminall в консоли (если есть доступ)
python clear_bp_tasks.py
```

**Что делает**: Удаляет все строки из таблицы `bp_tasks`, оставляя саму таблицу.

### Вариант B: Полная переинициализация (ЛУЧШИЙ ВАРИАНТ)

```bash
# Запустить переинициализацию
python reinit_bp_tasks.py
```

**Что делает**:
1. Удаляет все из `bp_completions` (записи о выполнении)
2. Удаляет все из `bp_tasks` (старые задания)
3. Вставляет 59 новых GTA5 RP заданий
4. Показывает результат

---

## 🚀 Способ 2: Прямо на Railway (если есть доступ к базе)

### Через Adminer (если включен)

1. Перейти на Railway → ваше приложение → Database
2. Открыть Adminer или Data Studio
3. Выбрать таблицу `bp_tasks`
4. Удалить все строки
5. Можно вручную вставить новые или запустить скрипт

### Через psql или другой SQL клиент

```sql
-- Только очистка
DELETE FROM bp_tasks;
COMMIT;

-- Или с переинициализацией (всё в одном запросе)
DELETE FROM bp_completions;
DELETE FROM bp_tasks;

INSERT INTO bp_tasks (name, category, bp_without_vip, bp_with_vip) VALUES
('3 часа в онлайне (можно выполнять многократно за день)', 'Легкие', 2, 4),
('Нули в казино', 'Легкие', 2, 4),
... (остальные 57 заданий)
COMMIT;
```

---

## 📋 Способ 3: Через CLI Railway

```bash
# Подключиться к базе данных
railway database connect

# Затем выполнить SQL команды
DELETE FROM bp_completions;
DELETE FROM bp_tasks;

-- Вставить новые задания
INSERT INTO bp_tasks (name, category, bp_without_vip, bp_with_vip) VALUES
...
```

---

## ⚠️ ВАЖНО

### После удаления

1. **Обязательно перезагрузить приложение** на Railway
2. Убедиться что `seed_bp_tasks.py` запустился в Procfile
3. Проверить что новые задания появились в таблице

### Проверка результата

```sql
-- Посмотреть количество
SELECT COUNT(*) FROM bp_tasks;

-- Посмотреть задания
SELECT * FROM bp_tasks;

-- Посмотреть выполнения
SELECT COUNT(*) FROM bp_completions;
```

---

## 🔄 Полный цикл обновления

1. **Локально**: Запустить `python reinit_bp_tasks.py` (если DATABASE_URL работает локально)
2. **Или вручную на Railway**:
   - Очистить таблицы через SQL
   - Запустить скрипт или вставить данные вручную
3. **Перезагрузить приложение** на Railway (Redeploy)
4. **Проверить**: Открыть web app и смотреть вкладку "Фарм BP"

---

## 🐛 Если всё ещё не работает

Проверить:
1. DATABASE_URL установлена в Railway?
2. Скрипт имеет права на DELETE/INSERT?
3. Таблицы существуют? (`SELECT * FROM information_schema.tables WHERE table_name='bp_tasks'`)
4. Нет ли блокировок? (другие соединения читают таблицу)
5. Кодировка данных правильная? (русские названия заданий)
