# Инструкция по исправлению ошибки telegram_id на Railway

## 📋 Краткая инструкция

Ошибка `psycopg2.errors.NumericValueOutOfRange: integer out of range` возникает у пользователей с большими Telegram ID потому, что база данных использует 32-битный `Integer` вместо 64-битного `BigInteger`.

## 🚀 Как исправить

### Шаг 1: Обновите код (уже сделано в текущей версии)

Код уже содержит автоматическую миграцию в `bot/models/init_db.py`. При запуске приложение автоматически проверит и исправит тип колонки.

### Шаг 2: Перезагрузите приложение на Railway

1. Откройте [Railway Dashboard](https://railway.app/dashboard)
2. Выберите ваш проект (telegram-bot)
3. Нажмите на **Services** → **bot** (или ваше приложение)
4. Нажмите кнопку **Redeploy** или **Deploy**
5. Дождитесь завершения развертывания

### Шаг 3: Проверьте логи

В логах приложения должно появиться сообщение:
```
✅ Successfully converted telegram_id to BigInteger
```

Или:
```
✅ Колонка уже имеет тип BigInteger (bigint), миграция не требуется
```

## ✅ Что произойдет после

- ✅ Пользователи с большими Telegram ID смогут добавлять товары
- ✅ Все существующие данные сохранятся
- ✅ Ошибка больше не будет появляться

## 🔧 Альтернатива: Ручная миграция (если требуется)

Если автоматическая миграция не сработала, вы можете выполнить ее вручную:

```bash
# На вашем локальном компьютере или сервере
python migrate_telegram_id.py
```

**Требуется**: Переменная окружения `DATABASE_URL` должна быть доступна.

## 📊 Диагностика

Если проблема не решена, проверьте тип колонки в базе данных:

```sql
SELECT data_type FROM information_schema.columns 
WHERE table_name = 'users' AND column_name = 'telegram_id';
```

Результат должен быть: **bigint**

## 📝 Технические детали

- **Проблема**: Telegram ID некоторых пользователей больше 2,147,483,647 (максимум для 32-bit signed integer)
- **Решение**: Использование 64-bit BigInteger (может хранить значения до 9,223,372,036,854,775,807)
- **Модель**: `User.telegram_id` в `bot/models/database.py` уже использует `BigInteger`
- **Миграция**: Автоматическая при инициализации БД на PostgreSQL
