## 🔴 СРОЧНО: Исправление ошибки telegram_id на Railway

### 📌 Проблема
```
psycopg2.errors.NumericValueOutOfRange: integer out of range
```
Ошибка возникает при добавлении товара для пользователей с большими Telegram ID.

### ✅ Решение (2 шага)

#### Шаг 1: Git commit и push
```bash
git add .
git commit -m "Fix: Auto-migrate telegram_id from Integer to BigInteger on PostgreSQL"
git push origin main
```

#### Шаг 2: Перезагрузить приложение на Railway
1. Откройте https://railway.app/dashboard
2. Выберите проект **telegram-bot**
3. Нажмите на сервис **bot**
4. Нажмите **Redeploy** (или обновите код)
5. Дождитесь завершения развертывания

### 🎯 Что произойдет
- Приложение автоматически проверит тип колонки `telegram_id`
- Если она `Integer`, будет выполнена миграция на `BigInteger`
- Все пользователи смогут добавлять товары без ошибок

### 📊 Проверка (необязательно)
В логах Railway должна появиться одна из строк:
- ✅ `Successfully converted telegram_id to BigInteger`
- ✅ `Колонка уже имеет тип BigInteger (bigint), миграция не требуется`

---

**Время на исправление**: ~2 минуты ⚡

Если что-то не сработало, смотрите `FIX_TELEGRAM_ID_RAILWAY.md` или `MIGRATION_TELEGRAM_ID.md`
