# 📚 Дополнительная документация

## 🔧 Быстрый старт

### Вариант 1: Автоматический запуск (Windows)

1. Дважды щелкните `start_bot.bat`
2. Бот автоматически:
   - Создаст виртуальное окружение
   - Установит зависимости
   - Проверит файл `.env`
   - Запустится

### Вариант 2: Автоматический запуск (Linux/Mac)

1. Сделайте скрипт исполняемым:
   ```bash
   chmod +x start_bot.sh
   ```

2. Запустите:
   ```bash
   ./start_bot.sh
   ```

### Вариант 3: Ручной запуск

```bash
# Активируйте виртуальное окружение
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate.bat на Windows

# Установите зависимости
pip install -r requirements.txt

# Запустите бот
python -m bot.main
```

## 📝 Шпаргалка команд

### Калькулятор перекупа

| Действие | Описание |
|----------|---------|
| `/start` | Запустить бота |
| 📊 Калькулятор перекупа | Открыть модуль |
| ➕ Купил | Добавить товар |
| 📋 Инвентарь | Просмотреть товары |
| 📈 Статистика | Показатели за период |
| 📜 История продаж | Все продажи |

### Управление арендой

| Действие | Описание |
|----------|---------|
| 🚗 Аренда | Открыть модуль |
| ➕ Добавить авто | Новый автомобиль |
| 🚗 Мои авто | Список авто |
| 💰 Сдал в аренду | Записать аренду |
| 📊 Статистика | Доход по авто |
| 🗑️ Удалить авто | Убрать из списка |

## 💡 Советы по использованию

### Категоризация товаров

Правильная категоризация помогает анализировать доходы по типам:

- **Аксессуар** - Маленькие предметы (телефоны, наушники, украшения)
- **Вещь** - Одежда, обувь, сумки
- **Квартира** - Недвижимость (квартиры, комнаты)
- **Дом** - Большая недвижимость
- **Автомобиль** - Машины, мотоциклы

### Расчет прибыли

Система автоматически считает:
```
Прибыль = Доход от продаж - Расходы на покупку
```

Если прибыль отрицательная - это убыток!

### Время окончания аренды

Вы можете вводить время несколькими способами:

1. **Абсолютное время** (формат HH:MM):
   ```
   18:30   (сегодня в 18:30)
   23:00   (сегодня в 23:00)
   ```

2. **Относительное время** (через +N часов):
   ```
   +2      (через 2 часа)
   +4      (через 4 часа)
   +8      (через 8 часов)
   ```

### Экспорт данных

Текущая версия хранит все данные в базе SQLite (`bot_data.db`).

Для резервной копии просто скопируйте файл:
```bash
cp bot_data.db bot_data_backup.db
```

## 🐛 Частые проблемы и решения

### Проблема: "Failed to connect to Telegram"

**Решение:**
- Проверьте интернет-соединение
- Убедитесь, что BOT_TOKEN правильный
- Проверьте, не заблокирован ли доступ к Telegram API

### Проблема: "Database is locked"

**Решение:**
- Закройте другие сеансы бота
- Перезагрузите приложение
- Удалите `bot_data.db` и перезапустите (будут потеряны данные!)

### Проблема: Уведомления не приходят

**Решение:**
- Убедитесь, что бот активен (окно с логами открыто)
- Проверьте, что система правильно установлена на московское время
- Фоновая задача должна работать постоянно

### Проблема: Товар не добавляется

**Решение:**
- Убедитесь, что вы ввели числовое значение для цены
- Все поля должны быть заполнены (кроме фото и комментария)
- Проверьте логи ошибок в консоли

## 🔐 Безопасность и приватность

### Защита данных

- Все данные хранятся локально на вашем сервере
- Ничего не синхронизируется с облаком
- Только вы имеете доступ к вашим финансовым данным

### Защита BOT_TOKEN

⚠️ **ВАЖНО:** Никогда не делитесь BOT_TOKEN!

```python
# ❌ НЕПРАВИЛЬНО - Никогда не делайте так!
BOT_TOKEN = "123456789:ABCdefGHIjklmnoPQRstuvWXYZabcDEf"

# ✅ ПРАВИЛЬНО - Используйте переменные окружения
BOT_TOKEN = os.getenv("BOT_TOKEN")
```

## 📊 Примеры расчетов

### Пример 1: Перекупа товара

```
Купили iPhone за 50,000₽
Продали за 60,000₽

Доход: 60,000₽
Расход: 50,000₽
Прибыль: 10,000₽ ✅
```

### Пример 2: Аренды автомобиля

```
Цена за час: 500₽
Количество часов: 4
Время окончания: 22:30

Расчет: 500₽ × 4 = 2,000₽
```

### Пример 3: Статистика

```
За день:
- Товар 1: Куплено 10,000₽, Продано 15,000₽ → Прибыль +5,000₽
- Товар 2: Куплено 20,000₽, Продано 18,000₽ → Убыток -2,000₽
- Аренда: 4 часа × 500₽ = 2,000₽

Итого: 5,000₽ - 2,000₽ + 2,000₽ = 5,000₽ ✅
```

## 🎯 Лучшие практики

### Финансовое управление

1. **Регулярно проверяйте статистику** - Это поможет отслеживать тренды
2. **Добавляйте комментарии** - Это поможет запомнить детали
3. **Загружайте фото** - Это поможет при спорах или проверках
4. **Делайте резервные копии** - На случай потери данных

### Управление арендой

1. **Устанавливайте правильное время** - Уведомления зависят от правильного времени
2. **Используйте единообразное наименование** - "Toyota Camry #1", "BMW X5 #2"
3. **Отслеживайте стоимость авто** - Важно для налогов и отчетности
4. **Регулярно смотрите статистику** - Какие авто приносят прибыль

## 🚀 Расширенное использование

### Запуск на сервере (Linux)

Для круглосуточной работы:

```bash
# Установите screen или tmux
sudo apt-get install screen

# Запустите бота в фоне
screen -S bot -d -m bash start_bot.sh

# Для входа в сеанс
screen -r bot

# Для выхода (Ctrl+A, затем D)
```

### Использование systemd (Linux)

Создайте файл `/etc/systemd/system/telegram-bot.service`:

```ini
[Unit]
Description=Telegram Finance Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/bot
ExecStart=/path/to/bot/venv/bin/python -m bot.main
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите:
```bash
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
```

## 📞 Контакты и поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте этот документ
2. Посмотрите логи приложения
3. Создайте Issue в репозитории
4. Свяжитесь через Telegram (если есть ссылка)

## 📜 История версий

### v1.0.0 (2024)
- ✅ Калькулятор перекупа
- ✅ Управление арендой
- ✅ Статистика за периоды
- ✅ Автоматические уведомления
- ✅ История продаж
- ✅ SQLite БД

### Планы на будущее
- 📋 Экспорт в CSV/Excel
- 📈 Графики прибыли
- 🔔 Уведомления о целях
- 💳 Интеграция с платежами
- 🌐 Веб-интерфейс
- 📱 Мобильное приложение

---

**Последнее обновление:** Декабрь 2024  
**Версия документации:** 1.0
