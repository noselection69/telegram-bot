# 🚀 Первый запуск бота

## Шаг 1: Получите BOT_TOKEN от BotFather

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. BotFather попросит название бота (например: "Finance Manager Bot")
4. BotFather попросит username (должно заканчиваться на "bot", например: "my_finance_bot")
5. BotFather выдаст вам **BOT_TOKEN** (длинная строка с символами и цифрами)

Пример BOT_TOKEN:
```
123456789:ABCdefGHIjklmnoPQRstuvWXYZabcDEf
```

## Шаг 2: Установите зависимости

### Windows (PowerShell)

```powershell
# Откройте PowerShell в папке проекта

# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
.\venv\Scripts\Activate.ps1

# Установите зависимости
pip install -r requirements.txt
```

### Windows (CMD)

```cmd
# Откройте CMD в папке проекта

# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
venv\Scripts\activate.bat

# Установите зависимости
pip install -r requirements.txt
```

### Linux/Mac

```bash
# Откройте терминал в папке проекта

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте его
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

## Шаг 3: Настройте .env файл

1. Скопируйте файл `.env.example` и переименуйте в `.env`:
   ```bash
   cp .env.example .env
   ```

2. Откройте `.env` в текстовом редакторе

3. Замените `your_bot_token_here` на полученный BOT_TOKEN:
   ```
   BOT_TOKEN=123456789:ABCdefGHIjklmnoPQRstuvWXYZabcDEf
   ```

4. Сохраните файл

## Шаг 4: Запустите бота

### Способ 1: Автоматический запуск (Windows)

Просто дважды щелкните файл `start_bot.bat`. Все сделается автоматически.

### Способ 2: Автоматический запуск (Linux/Mac)

```bash
chmod +x start_bot.sh
./start_bot.sh
```

### Способ 3: Ручной запуск

```bash
# Убедитесь, что виртуальное окружение активировано
# (должно быть (venv) в начале строки в терминале)

python -m bot.main
```

## Шаг 5: Протестируйте бота

1. Откройте Telegram
2. Найдите вашего бота по username (который вы вводили на шаге 1)
3. Нажмите кнопку "Start" или напишите `/start`
4. Вы должны увидеть приветственное сообщение с двумя вкладками:
   - 📊 Калькулятор перекупа
   - 🚗 Аренда

Если это произошло - **поздравляем**, бот работает! 🎉

## 📋 Проверочный список

- [ ] Получен BOT_TOKEN от BotFather
- [ ] Созданы виртуальное окружение и установлены зависимости
- [ ] Создан и заполнен файл `.env` с BOT_TOKEN
- [ ] Бот запущен (терминал показывает логи)
- [ ] Бот отвечает на команду `/start` в Telegram

## ⚠️ Возможные проблемы

### Проблема: "Не найден Python"

**Решение:** Убедитесь, что Python установлен и добавлен в PATH. Проверьте:
```bash
python --version
```

### Проблема: "ModuleNotFoundError: No module named 'aiogram'"

**Решение:** Убедитесь, что:
1. Виртуальное окружение активировано (в начале строки должно быть `(venv)`)
2. Вы установили зависимости:
   ```bash
   pip install -r requirements.txt
   ```

### Проблема: "Failed to connect to Telegram"

**Решение:**
1. Проверьте интернет-соединение
2. Убедитесь, что BOB_TOKEN правильный (скопируйте его еще раз от BotFather)
3. Проверьте, что BOT_TOKEN находится в `.env` файле и правильно:
   ```
   BOT_TOKEN=123456789:ABCdefGHIjklmnoPQRstuvWXYZabcDEf
   ```
   (без пробелов, без кавычек)

### Проблема: "KeyError: 'BOT_TOKEN'"

**Решение:** Файл `.env` не найден или неправильно заполнен. Проверьте:
1. Файл `.env` существует в папке проекта (на одном уровне с папкой `bot/`)
2. В файле есть строка: `BOT_TOKEN=your_token_here`
3. Нет лишних пробелов или кавычек

## 🎯 Следующие шаги

После успешного запуска бота:

1. **Прочитайте README.md** - Подробное описание функционала
2. **Изучите DOCS.md** - Расширенная документация и примеры
3. **Начните использовать** - Добавляйте товары и автомобили

## 💬 Нужна помощь?

1. Перепроверьте все шаги выше
2. Посмотрите в консоль/логи для ошибок
3. Создайте Issue в репозитории
4. Свяжитесь через Telegram

## 🔐 Безопасность

❌ **НИКОГДА не публикуйте:**
- Ваш BOT_TOKEN
- Файл `.env`
- Экран с открытым BOT_TOKEN

✅ **ВСЕГДА:**
- Храните BOT_TOKEN в `.env`
- Добавляйте `.env` в `.gitignore`
- Если случайно опубликовали токен - немедленно создайте новый в BotFather

---

**Готово?** Давайте приступим к работе с ботом! 🚀
