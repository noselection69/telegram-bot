#!/bin/bash
# 🚨 Railway Fix: Принудительное обновление БП заданий
# Запустите это в Railway Shell

cd /app
export PYTHONUNBUFFERED=1

echo "🔧 Принудительное обновление БП заданий на Railway..."
echo "================================================"

# Проверяем переменные окружения
if [ -z "$DATABASE_URL" ]; then
    echo "❌ ОШИБКА: DATABASE_URL не установлена!"
    echo "   Это должно быть установлено в Railway автоматически"
    exit 1
fi

echo "✅ DATABASE_URL найдена"
echo "🐍 Запускаем скрипт обновления..."

python force_update_railway_db.py

echo ""
echo "✅ Готово! Проверьте веб-интерфейс"
