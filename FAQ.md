# ❓ Часто задаваемые вопросы

## Web App

### Q: Как запустить Web App локально?
**A:** Запустите бота командой:
```bash
python -m bot.main
```
Веб-сервер автоматически запустится на `http://localhost:5000`

### Q: Почему Web App не открывается?
**A:** Проверьте:
1. Запущен ли бот (должен быть веб-сервер на порту 5000)
2. Не забыли ли вы `BOT_TOKEN` в `.env`
3. Очистите кэш Telegram

### Q: Как развернуть Web App на production?
**A:** 
1. Создайте облачный аккаунт (Heroku, DigitalOcean, AWS)
2. Установите зависимости: `pip install -r requirements.txt`
3. Запустите: `python -m bot.main`
4. Обновите URL в `bot/keyboards/keyboards.py`
5. Включите HTTPS (обязательно для Telegram)

### Q: Какие данные передаются от Telegram?
**A:** 
```javascript
- User ID (идентификатор)
- First name (имя)
- Last name (фамилия)
- Username (@username)
- Is bot (является ботом)
- Language code (язык)
- Is premium (премиум)
```

## Бот

### Q: Как добавить свой токен бота?
**A:** Создайте файл `.env` в корне проекта:
```
BOT_TOKEN=ваш_токен_тут
```

### Q: Как получить токен бота?
**A:** Напишите `@BotFather` в Telegram:
1. `/newbot` - создать нового бота
2. Выберите имя бота
3. Выберите username (@...)
4. Получите токен

### Q: Где хранятся данные?
**A:** В файле `bot_data.db` (SQLite база данных)

### Q: Как очистить базу данных?
**A:** 
```bash
rm bot_data.db
# Или удалите файл через файловый менеджер
```
База пересоздастся при следующем запуске.

### Q: Как добавить еще один токен (для тестирования)?
**A:** В `.env`:
```
BOT_TOKEN=основной_токен
TEST_BOT_TOKEN=тестовый_токен
```
Затем используйте в коде:
```python
bot_token = os.getenv('BOT_TOKEN')
```

## База данных

### Q: Как экспортировать данные?
**A:** Используйте `sqlite3`:
```bash
sqlite3 bot_data.db ".dump" > backup.sql
```

### Q: Как импортировать данные?
**A:** 
```bash
sqlite3 bot_data.db < backup.sql
```

### Q: Как сделать резервную копию?
**A:** 
```bash
cp bot_data.db bot_data.db.backup
```

### Q: Как обновить схему БД?
**A:** Отредактируйте файлы в `bot/models/` и пересоздайте БД

## Статистика

### Q: За какие периоды считается статистика?
**A:** 
- 📅 Сегодня (00:00 - 23:59)
- 📊 За неделю (последние 7 дней)
- 📈 За месяц (последние 30 дней)
- 📉 За все время

### Q: Почему статистика неверная?
**A:** Проверьте:
1. Часовой пояс (установлен на Moscow - Europe/Moscow)
2. Что вы добавляли товары / вели аренду

### Q: Как изменить часовой пояс?
**A:** В `bot/utils/timezone.py` измените:
```python
TIMEZONE = pytz.timezone('Europe/Moscow')
# На нужный вам, например:
# TIMEZONE = pytz.timezone('Asia/Yekaterinburg')
```

## Уведомления

### Q: Почему я не получаю уведомления?
**A:** Проверьте:
1. Включены ли уведомления в Telegram
2. Не выключены ли уведомления от бота
3. Правильно ли настроено время окончания аренды

### Q: Как отключить уведомления?
**A:** Откройте чат с ботом, нажмите на название, найдите "Уведомления" и выключите их

## Ошибки

### Q: ModuleNotFoundError: No module named 'aiogram'
**A:** Установите зависимости:
```bash
pip install -r requirements.txt
```

### Q: sqlalchemy.exc.OperationalError
**A:** Проблема с БД. Решение:
```bash
rm bot_data.db
python -m bot.main
```

### Q: ConnectionError: connect ECONNREFUSED
**A:** Проблема подключения. Проверьте:
1. Интернет соединение
2. Правильно ли указан `BOT_TOKEN`
3. Не забанили ли IP адрес Telegram

### Q: TimeoutError
**A:** Бот не может подключиться к Telegram. Проверьте:
1. Интернет соединение
2. Файрвол не блокирует ли доступ
3. Попробуйте VPN

## Разработка

### Q: Как добавить новый обработчик команды?
**A:** В файл `bot/handlers/` добавьте:
```python
@router.message(Command("mycommand"))
async def handle_mycommand(message: Message):
    await message.answer("Ответ")
```

### Q: Как добавить новую таблицу в БД?
**A:** 
1. Создайте модель в `bot/models/`
2. Добавьте в `Base.metadata.create_all()`
3. Пересоздайте БД

### Q: Как тестировать обработчики?
**A:** Используйте `pytest`:
```bash
pytest tests/
```

### Q: Как логировать события?
**A:** 
```python
import logging
logger = logging.getLogger(__name__)
logger.info("Это сообщение")
logger.error("Это ошибка")
```

## Производство

### Q: Как развернуть на Heroku?
**A:** 
```bash
# Создайте Procfile
echo "web: python -m bot.main" > Procfile

# Разверните
git push heroku main
```

### Q: Как развернуть на DigitalOcean?
**A:** 
1. Создайте Droplet (Ubuntu 20.04)
2. SSH подключитесь
3. Клонируйте репо: `git clone ...`
4. Установите: `pip install -r requirements.txt`
5. Запустите: `python -m bot.main`

### Q: Как использовать Docker?
**A:** 
```bash
# Создайте Dockerfile (см. DOCS.md)
docker build -t my-bot .
docker run my-bot
```

### Q: Как настроить monitoring?
**A:** Используйте инструменты:
- 📊 Prometheus - мониторинг метрик
- 📈 Grafana - визуализация
- 🔔 AlertManager - оповещения

## Безопасность

### Q: Безопасно ли хранить токен в .env?
**A:** 
- ✅ Да, если `.env` в `.gitignore`
- ❌ Нет, если залить в GitHub
- ✅ Для production используйте переменные окружения

### Q: Как защитить API?
**A:** Добавьте проверки:
```python
@app.route('/api/add-item', methods=['POST'])
def add_item():
    user_id = int(request.headers.get('X-User-ID', 0))
    if not user_id:
        return {'error': 'Unauthorized'}, 401
    # ... дальше код
```

### Q: Что если кто-то узнает мой токен?
**A:** 
1. Немедленно создайте новый токен через @BotFather
2. Удалите старый

## Производительность

### Q: Бот работает медленно
**A:** Проверьте:
1. Интернет соединение
2. Мощность компьютера
3. Размер БД (может быть слишком большой)
4. Оптимизируйте SQL запросы

### Q: Как увеличить скорость?
**A:** 
- Кэшируйте часто запрашиваемые данные
- Оптимизируйте SQL запросы
- Используйте асинхронные операции
- Добавьте индексы в БД

## Другое

### Q: Где хранится исходный код?
**A:** В папке `d:\bot` (главная папка проекта)

### Q: Как скачать проект?
**A:** 
```bash
git clone https://github.com/your-username/your-repo.git
cd your-repo
pip install -r requirements.txt
```

### Q: Как обновить проект?
**A:** 
```bash
git pull origin main
pip install -r requirements.txt --upgrade
```

### Q: Как внести изменения?
**A:** 
1. Создайте ветку: `git checkout -b feature/my-feature`
2. Сделайте изменения
3. Коммитьте: `git commit -m "Описание"`
4. Отправьте: `git push origin feature/my-feature`
5. Создайте Pull Request

---

**Не нашли ответ?** Откройте Issue на GitHub или напишите разработчику!
