[build]
builder = "dockerfile"

[deploy]
numReplicas = 1
startCommand = "python emergency_fix.py && python seed_bp_tasks.py && python migrate_postgresql.py && python run_migration.py && python -m bot.main"
restartPolicyMaxRetries = 5

[[volumes]]
sourcePathSuffix = "data"
mountPath = "/app/data"
