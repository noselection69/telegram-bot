[build]
builder = "dockerfile"

[deploy]
numReplicas = 1
startCommand = "python seed_bp_tasks.py && python migrate_postgresql.py && python add_buyprice_columns.py && python -m bot.main"
restartPolicyMaxRetries = 5

[[volumes]]
sourcePathSuffix = "data"
mountPath = "/app/data"
