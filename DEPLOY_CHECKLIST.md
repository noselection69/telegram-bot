# ✅ Чек-лист перед деплоем

## 📋 Подготовка кода

- [x] `requirements.txt` обновлен (gunicorn, psycopg2, python-decouple)
- [x] `Procfile` создан (для Railway/Heroku)
- [x] `Dockerfile` создан (для Docker)
- [x] `railway.toml` создан (конфиг для Railway)
- [x] `.gitignore` обновлен (секреты не коммитятся)
- [x] `.env.example` показывает нужные переменные

## 🔐 Безопасность

- [ ] **ВАЖНО:** Никогда не комитьте `.env` файл!
- [ ] **ВАЖНО:** Удалите `bot.log`, `bot_data.db` из git-истории если залили
- [ ] Проверьте что `.gitignore` содержит:
  - `.env`
  - `*.db`
  - `*.log`
  - `certs/`

```bash
# Если случайно залили .env:
git rm --cached .env
git commit -m "Remove .env from tracking"
```

## 📊 Выбор хостинга

### Railway.app ⭐⭐⭐⭐⭐ (РЕКОМЕНДУЕТСЯ)
- [ ] Перейти на https://railway.app
- [ ] Нажать "Start Project"
- [ ] Выбрать "Deploy from GitHub"
- [ ] Авторизоваться через GitHub
- [ ] Выбрать репозиторий `telegram-bot`
- [ ] Добавить сервисы:
  - [ ] Add Service → GitHub Repo
  - [ ] Add Service → PostgreSQL (опционально, можно SQLite)

### Render.com ⭐⭐⭐⭐
- [ ] Перейти на https://render.com
- [ ] "New +" → "Web Service"
- [ ] Подключить GitHub репозиторий
- [ ] Build: `pip install -r requirements.txt`
- [ ] Start: `python -m bot.main`

### VPS (Timeweb/Hoster.ru) ⭐⭐⭐⭐
- [ ] Заказать VPS (50-150₽/месяц)
- [ ] SSH доступ получен
- [ ] Запустить `deploy_vps.sh` скрипт

## 🔑 Переменные окружения

Добавь на хостинге эти переменные:

```
BOT_TOKEN = ваш_токен_бота
WEBHOOK_URL = https://your-app-name.railway.app
DATABASE_URL = postgresql://user:pass@host/db (или оставить sqlite)
FLASK_ENV = production
DEBUG = False
```

## 📱 GitHub Подготовка

- [ ] Репозиторий создан на GitHub
- [ ] Проект залит на GitHub:
```bash
git remote add origin https://github.com/YOUR_USERNAME/telegram-bot.git
git push -u origin main
```
- [ ] Репозиторий PUBLIC (необходимо для Railway)

## ✅ Перед финальным деплоем

- [ ] Локально протестировано: `python -m bot.main`
- [ ] Все функции работают:
  - [ ] /start - бот отвечает
  - [ ] Web App открывается
  - [ ] Добавление товара/авто работает
  - [ ] Уведомления приходят
- [ ] Нет критических ошибок в логах
- [ ] `.env` файл НЕ добавлен в git

## 🚀 После деплоя

- [ ] Проверить логи на хостинге
- [ ] Убедиться что бот работает: `/start` в телеграме
- [ ] Проверить Web App: кнопка открывается и работает
- [ ] Проверить БД: добавить тестовый товар/авто
- [ ] Убедиться что все данные сохраняются
- [ ] Настроить мониторинг (если есть)

## 📞 Если что-то не работает

### Бот не отвечает?
```bash
# Проверь логи на хостинге - поищи ошибку
# Обычно: неправильный BOT_TOKEN или проблема с сетью
```

### Web App не открывается?
```bash
# Проверь что WEBHOOK_URL правильный в .env на хостинге
# Должен быть: https://your-app-name.railway.app (без трейлинг слэша)
```

### БД не работает?
```bash
# Если используешь PostgreSQL:
# Проверь DATABASE_URL в переменных окружения
# Формат: postgresql://user:password@host:5432/dbname

# Если SQLite:
# Проверь что bot_data.db создана в правильной директории
```

### Часто крашится?
```bash
# Обычно: недостаточно памяти
# Railway автоматически перезагружает при сбое - это нормально
# Мониторь логи и смотри ошибки
```

---

## 🎉 Итого

**Твой бот готов к деплою!**

Выбирай хостинг из списка выше и следуй инструкциям. 

**Самый простой вариант:** Railway.app (5 минут настройки) ✨

Если возникнут проблемы - я помогу! 💪
