# 🐙 Подготовка GitHub для деплоя

## 📋 Что такое GitHub?

GitHub - это облачный сервис для хранения кода и совместной разработки. Используется для:
- ✅ Автоматического деплоя на хостинги (Railway, Render)
- ✅ Версионирования кода
- ✅ Работы в команде
- ✅ Бэкапа кода

---

## 🚀 Пошаговая подготовка

### Шаг 1: Создание GitHub аккаунта (если нет)

1. Перейди на **https://github.com**
2. Нажми **"Sign up"**
3. Введи email, пароль, username
4. Подтверди email

**Время: 2 минуты**

---

### Шаг 2: Создание репозитория на GitHub

1. Нажми на **"+"** в правом верхнем углу
2. Выбери **"New repository"**
3. Назови репозиторий: `telegram-bot` (или как хочешь)
4. Описание: `Telegram financial management bot`
5. **Выбери PUBLIC** ❗ (важно для Railway!)
6. Нажми **"Create repository"**

**Время: 1 минута**

---

### Шаг 3: Локальная подготовка проекта

Открой PowerShell/Terminal в директории проекта (`d:\bot`):

```bash
# Инициализируй Git в проекте
git init

# Добавь все файлы
git add .

# Создай первый коммит
git commit -m "Initial commit: Telegram bot ready for deployment"

# Переименуй ветку на main (если нужно)
git branch -M main
```

**Время: 1 минута**

---

### Шаг 4: Связывание локального проекта с GitHub

```bash
# Добавь удаленный репозиторий (замени YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/telegram-bot.git

# Загрузи код на GitHub
git push -u origin main
```

⚠️ Если просит пароль:
- Используй **Personal Access Token** вместо пароля
- Или авторизуйся через GitHub Desktop

**Время: 1 минута**

---

### Шаг 5: Проверка на GitHub

1. Перейди на https://github.com/YOUR_USERNAME/telegram-bot
2. Должны быть видны все файлы проекта
3. Должна быть папка `bot/`
4. Должен быть `requirements.txt`
5. Должен быть `Procfile`

**Если всё есть - отлично! ✅**

---

## 🔐 ВАЖНО: Защита секретов

### Что НЕ должно быть в GitHub:

❌ `.env` файл с BOT_TOKEN
❌ `*.db` файлы с данными
❌ `*.log` файлы
❌ `certs/` директория с сертификатами

### Проверка что в .gitignore:

Открой `.gitignore` - там должно быть:
```
.env
*.db
*.log
certs/
```

### Если случайно залил секреты:

```bash
# Удали файл из истории
git rm --cached .env
git commit -m "Remove .env from tracking"
git push
```

---

## 📝 Как работает Git workflow

```
1. Ты редактируешь файлы локально
   ↓
2. Добавляешь в stage: git add .
   ↓
3. Комитишь: git commit -m "Description"
   ↓
4. Загружаешь на GitHub: git push
   ↓
5. Railway видит push → автоматически деплоит
   ↓
6. Бот обновляется на хостинге!
```

---

## 📊 Git команды которые понадобятся

### Основные команды:

```bash
# Проверить статус
git status

# Добавить все изменения
git add .

# Сделать коммит
git commit -m "Description of changes"

# Загрузить на GitHub
git push

# Загрузить из GitHub (если работаешь с другого ПК)
git pull
```

### Если что-то пошло не так:

```bash
# Отменить последний коммит (но сохранить файлы)
git reset --soft HEAD~1

# Отменить все изменения в файле
git checkout -- filename

# Посмотреть историю коммитов
git log
```

---

## 🚀 После GitHub → Railway

1. На Railway нажми **"Deploy from GitHub"**
2. Авторизуйся через GitHub
3. Выбери репозиторий `telegram-bot`
4. Railway автоматически:
   - ✅ Клонирует репозиторий
   - ✅ Установит зависимости из `requirements.txt`
   - ✅ Запустит бота по инструкциям из `Procfile`
   - ✅ Даст URL https://app-name.railway.app

**Время: 2 минуты**

---

## 💡 GitHub Desktop (если не хочешь использовать консоль)

### Вариант 1: Через GitHub Desktop (графический интерфейс)

1. Скачай **GitHub Desktop** (https://desktop.github.com)
2. Авторизуйся
3. **"File" → "Clone repository"**
4. Выбери свой репозиторий
5. Клонируй на локальную машину
6. Редактируй файлы в VS Code
7. В GitHub Desktop нажми **"Commit"** и **"Push"**

**Проще для новичков!**

### Вариант 2: Через VS Code (встроенный Git)

1. В VS Code нажми на иконку Git (слева)
2. Нажми **"Initialize Repository"**
3. Коммитишь как обычно
4. Синхронизируешь через VS Code

---

## ✅ Финальный чек-лист

- [ ] GitHub аккаунт создан
- [ ] Репозиторий создан (PUBLIC!)
- [ ] Git инициализирован локально (`git init`)
- [ ] Все файлы добавлены (`git add .`)
- [ ] Первый коммит сделан
- [ ] Remote добавлен (`git remote add origin ...`)
- [ ] Код загружен на GitHub (`git push`)
- [ ] На GitHub видны все файлы проекта
- [ ] `.env` файл в `.gitignore` и не залит
- [ ] `requirements.txt` и `Procfile` на GitHub

---

## 🎉 Готово!

Теперь твой проект готов для автоматического деплоя на Railway! 

**Следующий шаг:** Перейди на Railway.app и нажми "Deploy from GitHub" 🚀

---

## 📚 Дополнительно

### Если нужно разобраться подробнее:
- Git основы: https://git-scm.com/book/ru/v2
- GitHub гайд: https://docs.github.com/ru
- GitHub Desktop: https://desktop.github.com

### Если есть ошибки при push:
```bash
# Проверь что всё правильно
git remote -v

# Если неправильный remote - замени
git remote set-url origin https://github.com/YOUR_USERNAME/telegram-bot.git

# Попробуй ещё раз
git push -u origin main
```

**Готов? Начнём! 🚀**
