# 🌐 Telegram Web App - Документация

## 📋 Что такое Telegram Web App?

Telegram Web App позволяет запускать полноценные веб-приложения прямо внутри Telegram с улучшенным интерфейсом.

## ✨ Особенности Web App

✅ **Встроенные формы** - красивые окна для ввода данных  
✅ **Тема Telegram** - автоматическая адаптация к теме юзера  
✅ **Натив-feel** - быстрое и отзывчивое приложение  
✅ **API Telegram** - доступ к данным пользователя  
✅ **Полноценный HTML/CSS/JS** - нет ограничений на функционал

## 🚀 Архитектура

```
Telegram
   ↓
Бот (aiogram)
   ├─ Кнопка "Web App"
   └─ Открывает веб-приложение
      ↓
   Веб-сервер (Flask)
      ├─ /                    - Главная страница (HTML)
      ├─ /static/style.css    - Стили
      ├─ /static/app.js       - JavaScript
      ├─ /api/add-item        - API для добавления товара
      ├─ /api/sell-item       - API для продажи
      ├─ /api/add-car         - API для авто
      └─ /api/rent-car        - API для аренды
```

## 📁 Структура файлов Web App

```
bot/web/
├── __init__.py
├── app.py                   # Flask приложение и API
├── templates/
│   └── index.html          # HTML интерфейс
└── static/
    ├── style.css           # Стили
    └── app.js              # JavaScript логика
```

## 🔧 Конфигурация

### Локальное тестирование

Когда вы запускаете бота локально, используется:
```
URL: http://localhost:5000
```

### Развертывание на сервер

Для production нужно:

1. **Развернуть на облачном сервере** (например, AWS, Heroku, DigitalOcean)
2. **Обновить URL в клавиатуре**:

```python
WebAppInfo(url="https://your-domain.com")
```

3. **Настроить HTTPS** (обязательно для Telegram)

## 📤 Данные от пользователя

Telegram автоматически передает данные пользователя:

```javascript
const tg = window.Telegram.WebApp;

// Получить ID пользователя
const userId = tg.initDataUnsafe?.user?.id;

// Получить имя
const firstName = tg.initDataUnsafe.user.first_name;

// Получить username
const username = tg.initDataUnsafe.user.username;
```

## 🔌 API Endpoints

### POST /api/add-item
Добавить товар

**Требуемые поля:**
```json
{
    "name": "iPhone 13",
    "category": "ACCESSORY",
    "price": 50000,
    "comment": "В хорошем состоянии"
}
```

**Headers:**
```
X-User-ID: 123456789
Content-Type: application/json
```

**Ответ:**
```json
{
    "success": true,
    "message": "Товар добавлен!",
    "item_id": 1
}
```

### POST /api/sell-item
Продать товар

```json
{
    "item_id": 1,
    "price": 60000
}
```

### POST /api/add-car
Добавить автомобиль

```json
{
    "name": "BMW X5",
    "cost": 500000
}
```

### POST /api/rent-car
Записать об аренде

```json
{
    "car_id": 1,
    "price_per_hour": 500,
    "hours": 4,
    "end_time": "22:30"
}
```

## 🎨 Интерфейс

### Вкладки
- 📊 **Перекупа** - добавление и продажа товаров
- 🚗 **Аренда** - управление автомобилями и записями об аренде

### Формы
- **Добавить товар** - название, категория, цена, комментарий
- **Добавить авто** - название, стоимость
- **Сдать в аренду** - цена/час, часы, время окончания

### Кнопки
- ✅ Успешно (зеленая)
- ❌ Ошибка (красная)
- ❌ Отмена (серая)
- ➕ Добавить (фиолетовая)

## 🌙 Теоретические возможности

Можно добавить:

```javascript
// Темный режим
tg.setBackgroundColor('#1e1e1e');

// Цвет заголовка
tg.setHeaderColor('#667eea');

// Главная кнопка
tg.MainButton.setText('Готово');
tg.MainButton.onClick(() => {
    // Обработка
});

// Кнопка обратно
tg.BackButton.show();
tg.BackButton.onClick(() => {
    closeWebApp();
});

// Уведомления
tg.HapticFeedback.impactOccurred('medium');
```

## ⚙️ Переменные окружения

Если требуются для Web App, добавьте в `.env`:

```
WEB_APP_URL=https://your-domain.com
WEB_APP_PORT=5000
```

## 📝 Примеры использования

### Добавление товара через Web App

1. Откройте бот в Telegram
2. Нажмите "📊 Калькулятор перекупа"
3. Выберите "➕ Купил (Web App)"
4. Заполните форму
5. Нажмите "✅ Добавить"
6. Товар добавится автоматически

### Сдача авто в аренду

1. Откройте "🚗 Аренда"
2. Выберите "➕ Добавить авто (Web App)" (если нет авто)
3. Или откройте авто и сдайте в аренду
4. Заполните форму
5. Готово!

## 🔒 Безопасность

### Защита данных
- ✅ X-User-ID передается в заголовке
- ✅ Все данные валидируются на сервере
- ✅ HTTPS для production

### CORS
- ✅ Настроены правильно для localhost
- ⚠️ На production требуется правильная конфигурация

## 🚀 Развертывание

### Вариант 1: Heroku (легко)

```bash
# Создайте Procfile
web: python -m bot.main

# Разверните
git push heroku main
```

### Вариант 2: DigitalOcean (VPS)

```bash
# Установите зависимости
pip install -r requirements.txt

# Запустите с gunicorn
gunicorn -b 0.0.0.0:5000 bot.web.app:app
```

### Вариант 3: Docker

```dockerfile
FROM python:3.9
WORKDIR /app
COPY . .
RUN pip install -r requirements.txt
CMD ["python", "-m", "bot.main"]
```

## 📊 Мониторинг

### Логи
```bash
# Смотрите логи веб-сервера
tail -f logs/web.log

# Смотрите логи бота
tail -f logs/bot.log
```

### Метрики
Добавить отслеживание:
- Количество добавленных товаров
- Количество продаж
- Активные аренды
- Время отклика API

## 🐛 Решение проблем

### Проблема: Web App не открывается

**Решение:**
- Проверьте, запущен ли веб-сервер (порт 5000)
- Проверьте URL в кнопке
- Очистите кэш Telegram

### Проблема: Данные не сохраняются

**Решение:**
- Проверьте логи сервера
- Убедитесь, что БД работает
- Проверьте X-User-ID в запросе

### Проблема: Стили не загружаются

**Решение:**
- Проверьте пути к CSS/JS файлам
- Очистите кэш браузера
- Перезагрузите Web App

## 📚 Дополнительные ресурсы

- [Telegram Bot API Docs](https://core.telegram.org/bots/api)
- [Telegram Web App Docs](https://core.telegram.org/bots/webapps)
- [Flask Documentation](https://flask.palletsprojects.com/)

## ✅ Чеклист для развертывания

- [ ] Веб-сервер работает локально
- [ ] API endpoints тестируются
- [ ] Стили загружаются правильно
- [ ] Данные сохраняются в БД
- [ ] HTTPS настроен (для production)
- [ ] URL обновлен в кнопках
- [ ] Логирование настроено
- [ ] Мониторинг настроен

---

**Версия:** 1.0  
**Последнее обновление:** Декабрь 2024
